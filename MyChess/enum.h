#pragma once


enum BorderColorCell {
	GREEN, 
	RED, 
	BLUE, 
	WHITE
};

enum TeamColor {
	WHITE_TEAM,
	BLACK_TEAM
};

enum StateMouse {
	FREE,
	PUSH,
	RELEASE
};

enum KillingKing {
	WHITE_KING,
	BLACK_KING,
	LIVE_BOTH
};

enum CurrentTeamMove {
	WHITE_MOVE,
	BLACK_MOVE
};